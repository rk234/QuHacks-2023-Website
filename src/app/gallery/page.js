import styles from "./page.module.css"

export default function GalleryPage() {
    return (
        <main>
            Gallery page goes here (probably copy basic styles from timeline or home page)
        </main>
    )
}